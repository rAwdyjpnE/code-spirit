# CODE SPIRIT 🐍

**Code Spirit** — это интерактивная платформа для обучения Python в режиме реального времени. Админ видит код студентов посимвольно (Live-режим), может раздавать задачи и запускать автотесты в изолированной среде.

![License](https://img.shields.io/badge/license-MIT-blue.svg)
![Python](https://img.shields.io/badge/python-3.11-blue)
![React](https://img.shields.io/badge/react-18-blue)
![Docker](https://img.shields.io/badge/docker-ready-green)

## ✨ Возможности

- **Real-time**: Админ видит, как студент печатает код.
- **Docker Sandbox**: Код студентов выполняется в безопасных, изолированных контейнерах.
- **Автопроверка**: Мгновенный запуск тестов и отображение результатов.
- **WebSocket**: Мгновенная синхронизация статусов и задач.

---

## 🚀 Установка и Запуск

Для работы проекта используется гибридный подход: Бэкенд в Docker, Фронтенд локально (для скорости разработки).

### 📋 Требования

Перед началом убедитесь, что у вас установлены:
1.  **Docker Desktop**: [Скачать для Windows/Mac/Linux](https://www.docker.com/products/docker-desktop/)
2.  **Node.js (v18+)**: [Скачать](https://nodejs.org/)
3.  **Git**: [Скачать](https://git-scm.com/)

---

### Шаг 1: Клонирование и Настройка

1.  Откройте терминал (PowerShell или CMD) и склонируйте репозиторий:
    ```bash
    git clone https://github.com/ваш-юзернейм/code-spirit.git
    cd code-spirit
    ```

2.  Создайте файл настроек:
    Скопируйте `.env.example` в `.env` (или создайте `.env` вручную с содержимым из примера).

### Шаг 2: Запуск Бэкенда (Docker)

В корневой папке проекта выполните:

1.  **Запустите базу данных и сервер:**
    ```bash
    docker-compose up -d
    ```
    *Флаг `-d` запускает процесс в фоновом режиме.*

2.  **⚠️ ВАЖНО: Сборка образа для проверки кода**
    Чтобы система могла проверять решения студентов, нужно один раз собрать образ воркера. Выполните эту команду в корне проекта:
    ```bash
    docker build -t code-spirit-worker -f docker/Dockerfile.worker .
    ```

3.  **Загрузка задач в базу данных:**
    ```bash
    docker-compose exec backend python scripts/seed_tasks.py
    ```

### Шаг 3: Запуск Фронтенда (Локально)

Откройте **новый терминал**, перейдите в папку `frontend` и запустите сервер:

```bash
cd frontend
npm install
npm run dev
```

После этого проект доступен по адресу: **http://localhost:5173**

---

## 🌐 Доступ из интернета (Tuna)

Чтобы друзья или студенты могли подключиться к вашему локальному серверу, используйте **Tuna Desktop**.

👉 **[Инструкция по настройке Tuna](docs/TUNNELING.md)**

---

## 🛠 Устранение проблем

**Ошибка: `404 Client Error ... code-spirit-worker: Not Found`**
Вы забыли собрать образ воркера. Выполните команду из Шага 2 пункт 2.

**Ошибка: `TypeError: add() missing arguments`**
Убедитесь, что вы пересобрали контейнеры после обновления кода:
`docker-compose up --build -d`

---

*Developed with @JasonVUrhyz❤️.*
