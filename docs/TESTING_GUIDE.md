# Руководство по тестированию Code Spirit

Это пошаговый сценарий для проверки всех ключевых функций платформы.

## 1. 🖥️ Подготовка

1.  **Запустите Backend и Базу данных** (в первом терминале):
    ```bash
    docker compose up --build db backend
    ```
2.  **Загрузите задачи** (во втором терминале):
    ```bash
    docker compose exec backend python scripts/seed_tasks.py
    ```
3.  **Запустите Frontend** (во втором терминале):
    ```bash
    cd frontend && npm run dev
    ```
4.  Откройте браузер (рекомендуется Chrome или Firefox).
5.  Откройте **два окна**: одно обычное, второе в режиме Инкогнито.

## 2. 👩‍🏫 Сценарий Преподавателя

1.  В **первом окне** перейдите на `http://localhost:5173`.
2.  **Нажмите** "Я Преподаватель".
3.  **Нажмите** "Создать новую сессию".
4.  **Введите** пароль (например, `12345`) и нажмите "Создать и войти".
    *   **Результат:** Вы на Дашборде. В URL виден ID сессии.
5.  **Скопируйте** ссылку для подключения студентов.

## 3. 👨‍🎓 Сценарий Студента

1.  Во **втором окне (Инкогнито)** вставьте скопированную ссылку.
2.  **Введите** имя (например, "Алексей") и нажмите "Начать".
    *   **Результат:** Открылось рабочее пространство. Статус "Online". Задания пока нет.

## 4. ⚡ Проверка Real-time

1.  **В окне Преподавателя:** Мгновенно должна появиться карточка "Алексей" с зеленой точкой.
2.  **В окне Преподавателя:** Нажмите кнопку "Раздать задания".
3.  **В окне Студента:** **Мгновенно** (без перезагрузки) слева появится текст задания.

## 5. 👁️ Проверка Live-просмотра

1.  **В окне Преподавателя:** Кликните на карточку студента "Алексей".
2.  **В окне Студента:** Начните печатать код в редакторе.
3.  **В окне Преподавателя:** Вы должны видеть, как символы появляются в реальном времени. Статус студента должен быть "Typing...".

## 6. ✅ Проверка успешного решения

1.  **В окне Студента:** Введите правильный код для задачи и нажмите "Запустить".
2.  **Результат:** Внизу появятся зеленые галочки и надпись "Все тесты пройдены!".
3.  **В окне Преподавателя:** Вернитесь на дашборд. У карточки студента должна появиться **зеленая обводка**.

## 7. ❌ Проверка небезопасного кода

1.  **В окне Студента:** Введите опасный код, например `import os`.
2.  Нажмите "Запустить".
3.  **Результат:** Появится ошибка "Security Error".
```